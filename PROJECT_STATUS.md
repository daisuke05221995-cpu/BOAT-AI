# BOAT AI 引継ぎメモ

## 現在の状態

- Repository: `daisuke05221995-cpu/BOAT-AI`
- Branch: `main`
- Current version: `0.13.0`
- Android package: `jp.boatai.app`
- Release workflow: `.github/workflows/release.yml`
- Update source: GitHub Releases
- Latest Release: `v0.13.0`

## 実装済み

- 予想・結果・損益の3タブ
- 開催場カード、場別・全国一括購入記録
- 3連単4点予想、購入記録、結果照合、日別仮想収支
- PC版/スマホ版の二系統オッズ取得と60秒自動更新
- 欠落結果の公式結果ページ補完
- 会場×艇番勝率を継続学習する端末内学習プロファイル
- 会場×風速帯×実進入コースの継続学習と予想補正
- 進入変化・強風を含む外れ理由表示
- 過去日の当時予想・的中率・仮想収支
- 署名固定の自動Releaseとアプリ内更新
- 2021-09-22〜2026-09-21の5年分公式成績ベースラインを予想へ接続
- 5年ベースライン導入後の事前予想を条件別に自動検証
- 内部ではAIランク・会場・1着軸・複合条件の成績を学習し、弱い条件を補正
- ユーザー向け最終判断は「購入推奨 / 見送り」の2択
- 購入推奨/見送り判定を結果確定前の予想保存時点で固定記録
- 全国一括・場別一括で「購入推奨のみ」「見送りも含む」を選択可能
- 損益タブで「購入推奨のみ」「全予想」「見送り判定」を別集計
- 購入推奨のみの的中率・購入額・払戻・損益・回収率を算出
- 詳細分析を会場 / 判定 / 1着軸で表示
- CSVへ購入判定・判定理由を出力
- データ取得元・取得件数・失敗理由・取得時刻を確認できる診断カード
- 1レース1,000〜3,000円の4点自動配分、本線/中穴/超穴、買い目別根拠
- オッズ変動表示と締切前の購入推奨/見送り再判定
- 日別グラフ、期間別・会場別・判定別・1着軸別集計、連勝/連敗
- JSONバックアップ/復元、CSV出力、削除前確認
- 締切・結果・学習・アプリ更新通知、ダークモード
- 公式API、クラウド同期、認可済み投票導線へ差し替える接続インターフェース
- versionCode更新時に画面上の予想実績・実購入履歴を自動初期化
- 5年ベースラインと端末内レース学習は更新後も保持
- 旧バージョンの確定予想から得た弱点補正は内部PerformanceMemoryへ退避し、実績表示リセット後も予想補正へ継続利用
- BOAT RACE公式トップを先に開いてCookieを保持した同一セッションから3連単オッズを取得
- 公式オッズ取得を最大3回再試行し、PC版失敗時はスマホ版へフォールバック
- 公式PC版のrowspan付き3連単オッズ表を論理セルへ展開して解析し、従来の全table探索もフォールバックとして維持

## v0.13.0 購入推奨 / 見送り仕様

- 画面には90/91/92のような細かなAI点数やS/A/Bランクを最終判断として表示しない。
- 内部の0〜100連続スコアは、選手力・コース・機力・ST・展示・学習補正などを統合する計算値として残す。
- 基本の購入基準は内部補正後スコア70以上。
- 過去実績から十分な件数があり成績の悪い条件は、内部スコアに関係なく見送り側へ補正できる。
- 最終判断は `RaceRecommendation.BUY`（購入推奨）または `RaceRecommendation.SKIP`（見送り）。
- 判定理由も予想保存時点で `PredictionRecord` に保存する。
- 結果が出た後に判定を作り直さないため、「その時点で本当に推奨していたレース」の成績だけを正しく集計できる。
- オッズ取得後の最終判断も購入推奨 / 見送りの2択とし、「慎重」は廃止。
- 個別購入は見送り判定でもユーザーが手動で登録可能。一括では推奨のみ、または見送り含むを選べる。

## v0.13.0 損益・集計仕様

- `購入推奨だけ買った場合`: 推奨判定だった確定予想のみを集計。
- `全予想を買った場合（比較用）`: 見送りを含む全ての評価対象予想を集計。
- `見送り判定の成績（検証用）`: 見送ったレースだけを追跡し、見送り判断の妥当性を検証。
- 各集計でレース数、的中数、的中率、購入額、払戻額、損益、回収率を表示。
- 日別でも購入推奨のみと全予想を並べて比較できる。
- 詳細分析では会場 / 判定 / 1着軸を切り替え可能。
- 見送りによって回避できた仮想損失がプラスの場合は参考値として表示。
- CSVではAIランク列の代わりに購入判定・判定理由を出力する。

## v0.13.0 検証

- Live race API schema確認成功
- `RecommendationDecisionTest` を追加し、強い条件=購入推奨、弱い条件=見送りを検証
- `ProfitAnalyticsTest` を更新し、購入推奨のみ / 全予想 / 見送りの分離集計を検証
- Unit tests成功
- Android lint成功
- Debug APKビルド成功
- Release workflowから署名付きAPK公開成功
- GitHub Release `v0.13.0` 公開成功
- Release APK: `BOAT-AI-v0.13.0.apk`
- SHA256ファイルも同時公開済み
- 完全自動投票は実装対象外。購入記録と公式ページへの手動導線を維持する。

## 更新時の実績リセット仕様（v0.12.1〜）

- `BuildConfig.VERSION_CODE` が前回起動時と変わった最初の起動だけ実績リセットを実行する。
- リセット対象: `boat_ai_bets/records`（実購入履歴）、`boat_ai_predictions/prediction_records`（予想実績）。
- 同じバージョンを通常起動するだけではリセットしない。
- 画面上の的中率・購入額・払戻・損益は新バージョンで0から計測する。
- 更新前の確定予想から計算した条件別成績は `boat_ai_performance_memory` へ集約し、予想の弱点補正だけ内部で引き継ぐ。
- 5年分公式成績ベースラインと `boat_ai_learning` の端末内レース学習は削除しない。
- これにより「新バージョンの実績だけを表示」と「過去の学習知識を捨てない」を分離する。

## オッズ取得仕様（v0.12.1〜）

- 取得元はBOAT RACE公式の3連単オッズページ。
- 単発アクセスではなく、まず公式トップを開き、同じJsoup Session/Cookieで対象オッズページを取得する。
- User-Agent、Accept、Accept-Language、Referer、no-cacheを付与し、最大3回再試行する。
- PC版を優先し、失敗時はスマホ版へフォールバックする。
- 現行PC版の `rowspan` を使ったオッズ表を展開して `1-2-3` 形式へ復元する。
- 現行構造が変わった場合に備え、従来の全table探索式パーサーも残す。
- オッズ取得元、取得点数、成功した試行回数、失敗理由を診断表示する。
- 開催中の60秒自動更新を維持する。

## 学習仕様

結果取得前（または学習反映前）に予想と購入判定を保存してから結果を学習する。結果を予想に混ぜない。
端末内のSharedPreferences `boat_ai_learning` に、学習済みレースID、会場×艇番の出走・1着回数、会場×風速帯×実進入コースの出走・1着回数を保存。
風速帯は0〜2m / 3〜4m / 5m以上。同条件4件未満では追加補正を行わない。
`PredictionEngine.installLearningProfile` で次回予想の選手スコアへ補正する。
旧v0.7以前の学習JSONに新フィールドが無くても空Mapとして読み込み互換性を維持する。

## 5年分の過去学習ベースライン

- 対象期間: `2021-09-22` 〜 `2026-09-21`
- 元データ: BOAT RACE公式の成績Kファイル
- 完成データ: `app/src/main/assets/historical_learning.json`
- 最終集計: 271,279レース / 1,826日 / 欠損0日
- 会場×艇番バケット: 138
- 会場×風速帯×実進入コースバケット: 414
- 生レースをAPKへ大量保存せず、予想補正に必要な集計値のみAPKへ同梱する。
- `.github/workflows/historical-learning.yml` で2021〜2026を6期間に分けて並列集計し、`scripts/merge_historical.py` で結合・検証する。
- 最終結合時に対象期間の連続性、15万レース以上、会場×艇番・風速×進入のバケット数を検証してから自動コミットする。
- `LearningStore` は `historical_learning.json` をアプリ起動時に自動読込し、端末内の新規学習データと合算して `PredictionEngine` へ渡す。
- ベースライン最終日以前のレースは端末側で再学習しないため、5年分データとの二重加算を防止する。
- 過去バージョンの端末学習がベースライン期間と重複している場合は、一度だけ旧集計をクリアして5年ベースラインへ安全に移行する。
- AI学習済みレース数は「271,279レース + ベースライン後の端末学習件数」を表示する。

## 予想成績の自動検証・フィードバック

- 評価対象は「結果が出る前かつ締切前にアプリが保存できた予想」のみ。過去日を後から開いて生成した予想は `evaluationEligible=false` として補正学習から除外する。
- 内部ではAIランク、会場、1着軸、会場×AIランク×1着軸などの条件別成績を引き続き保持し、購入判定の精度向上に利用する。
- 仮想成績は3連単4点を各300円で購入した想定の的中率・回収率・損益を使用する。
- 補正開始の最低件数は、会場24件、AIランク30件、1着軸30件、会場×AIランク×1着軸16件。
- 条件別回収率に応じて内部評価を減点し、複数条件の合計減点は最大-12ptに制限する。
- 十分な件数があり「回収率55%未満かつ的中率8%未満」の条件は見送り対象にできる。
- 見送りになってもシャドー予想自体は保存・結果評価するため、見送り判断の成績も検証できる。
- v0.13.0以降、これらの細かな内部評価は画面の最終判断として見せず、「購入推奨 / 見送り」へ変換して表示する。

## 続きの始め方

新しいチャットで「GitHubの `daisuke05221995-cpu/BOAT-AI` と `PROJECT_STATUS.md` を確認して続けて」と依頼する。
実機不具合は、バージョン番号・場名・レース番号・画面スクリーンショットを添える。

## 今後の確認

- v0.13.0を実機へ更新し、予想一覧が「購入推奨 / 見送り」の2択表示になっていることを確認
- 全国・場別一括で「購入推奨のみ」「見送りも含む」の選択件数が正しいことを確認
- 損益タブで購入推奨のみ / 全予想 / 見送りの3集計が0から蓄積されることを確認
- 実績が30〜100件程度たまったら、購入推奨のみの回収率が全予想より改善しているかを評価し、必要なら購入基準を自動最適化する
- 実機画面を基準に文字量とカード間隔を微調整
